import 'package:flutter/material.dart';

class NewLocationItem extends StatelessWidget {
  const NewLocationItem({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
